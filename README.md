# Awesome AGY Subagents

The largest open-source collection of **AGY-compatible subagents and tools**.

[AGY](https://antigravity.google/docs/subagents/) is the Antigravity CLI
(`agy`) — Google's terminal AI coding agent. An AGY **subagent** is a single
Markdown file whose body is a system prompt and whose YAML frontmatter carries
runtime configuration. This repository aggregates, converts, validates and
categorizes subagents from hundreds of public repositories so they can be used
immediately in AGY (and any frontmatter-subagent harness).

> **1,186 converted subagents · 20 contributing repositories · 113 documented
> tools · 333 repositories discovered**

---

## Quick start

1. Copy any agent into your AGY project:

   ```bash
   mkdir -p .gemini/agents
   cp agents/security/security-auditor.md .gemini/agents/
   ```

2. Invoke it from `agy` by its `name`:

   ```
   > ask the security-auditor subagent to review src/auth.py
   ```

3. Or install the entire collection at once (every agent is already
   validated and categorized under `agents/<category>/`):

   ```bash
   cp -r agents/* .gemini/agents/
   ```

Every agent is ready to use — no conversion, renaming, or manual work needed.

---

## Repository layout

```
agents/           # 1,186 converted AGY subagents, organized by category
  backend/ frontend/ security/ devops/ ai/ mobile/ testing/
  research/ writing/ architecture/ ... (26 categories)
tools/            # 113 documented tools (core AGY tools + MCP integrations)
  core/  mcp/
workflows/        # Multi-agent workflow definitions
examples/         # Composition / usage examples
templates/        # Authoring templates for new agents & plugins
docs/             # Specification, importing guide, compatibility model
validation/       # JSON Schema for the AGY format + validation results
scripts/          # Discovery, import, convert, validate, dedupe, report, export
  discover/ import/ convert/ validate/ dedupe/ report/ export/
metadata/         # Machine-readable registries (agents.json, summary.json, ...)
reports/          # Generated human-readable reports
exports/          # agents.json + agents.csv exports
imports/          # Source registry (cloned repos are git-ignored)
```

---

## Every agent carries

Each file in `agents/` is a complete AGY subagent with:

- **Name** — a unique, invocation-ready slug
- **Description** — how the parent agent decides when to call it
- **Category** + **tags**
- **Original repository, author, license, source URL, and file path**
- **Required / optional tools** and **required MCP servers**
- **AGY compatibility score** and status
- **The converted AGY system prompt**
- **Validation status** and **import timestamp**

See [`docs/AGY-SPEC.md`](docs/AGY-SPEC.md) for the full format, and
[`metadata/agents.json`](metadata/agents.json) for the machine-readable registry.

---

## Statistics

Generated by `scripts/report.py` — see [`reports/summary.md`](reports/summary.md).

| Metric | Value |
|---|---|
| Repositories discovered | 333 |
| Repositories scanned | 25 |
| Agents discovered (pre-dedupe) | 1,572 |
| **Agents converted** | **1,186** |
| Fully AGY-compatible | 889 |
| Requires MCP | 145 |
| Needs tool mapping | 144 |
| Requires manual conversion | 8 |
| Conversion success rate | 75.0% |
| Average compatibility score | 94.8 |
| Documented tools | 113 |
| Categories | 26 |

Top contributing repositories (full list in
[`reports/repositories.md`](reports/repositories.md)):

- `wshobson/agents` — 202 agents
- `VoltAgent/awesome-claude-code-subagents` — 158 agents
- `VoltAgent/awesome-codex-subagents` — 172 agents
- `ankitmundada/awesome-gemini-cli-subagents` — 130 agents
- `0xfurai/claude-code-subagents` — 138 agents
- `0xSteph/pentest-ai-agents` — 53 agents
- …and many more

---

## Importing & maintenance

The importer is fully re-runnable. To refresh the collection:

```bash
# Full pipeline (discover → clone → convert → dedupe → validate → report → export)
python3 scripts/run_all.py

# Or individual stages
python3 scripts/run_all.py --discover --clone   # find & fetch new repos
python3 scripts/run_all.py --convert            # re-extract & convert
```

- **Discovery** queries the GitHub search API for agent-bearing repositories
  and writes `metadata/discovered_repos.json`.
- **Import** shallow-clones matching repositories into `imports/sources/`
  (git-ignored).
- **Convert** scans for agent definitions in Markdown, TOML, YAML and JSON,
  parses frontmatter, maps tools, categorizes, and emits canonical AGY files.
- **Dedupe** merges exact and near-duplicate agents while preserving every
  original source for attribution.
- **Validate** checks every agent against the spec.
- **Report / Export** regenerate summary stats and `exports/`.

To contribute a new upstream repository, add it to `KNOWN_REPOS` in
`scripts/discover.py` and re-run `scripts/run_all.py`. See
[`docs/importing.md`](docs/importing.md) and
[`CONTRIBUTING.md`](CONTRIBUTING.md).

---

## License

Collection and tooling: **MIT** (see [LICENSE](LICENSE)). Individual imported
agents retain their upstream licenses, recorded in each agent's
`agy.sources[].license` field.
